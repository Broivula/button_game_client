<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">@color/colorSplashBackground</item>
        <item name="android:windowBackground">@color/colorSplashBackground</item>
    </style>

    <!-- Styling for the dialog in username entry -->
    <style name="UserCreationTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">@color/colorUserCreationBackground</item>
    </style>

    <style name="UserCreationDialogTheme">
        <item name="android:background">@color/colorUsernameDialogBackground</item>
        <item name="android:windowEnterAnimation">@anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@anim/anim_slide_up_out</item>
    </style>


    <style name="DialogEditTextStyle" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="colorControlNormal">@color/colorSecondary</item>
        <item name="colorControlActivated">@color/colorSecondaryLighter</item>
        <item name="colorControlHighlight">@color/colorSecondaryLighter</item>
    </style>

    <!--    Styling for game over dialog     -->
    <style name="GameOverDialogTheme">
        <item name="android:background">@color/colorUsernameDialogBackground</item>
        <item name="android:windowEnterAnimation">@anim/slide_in_up</item>
        <item name="android:windowExitAnimation">@anim/anim_fade_out</item>
    </style>

    <!--    General styling     -->
    <style name="TextOutlineStyle">
        <item name="android:paddingLeft">4px</item>
        <item name="android:paddingBottom">4px</item>
        <item name="android:textColor">#ffffffff</item>
        <item name="android:textSize">12sp</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">1</item>
    </style>

</resources>
